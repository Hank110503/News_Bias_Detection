# Training-focused requirements for the current src/bias_vl pipeline.
# Generated from direct imports in the training/eval/data-conversion scripts
# and aligned with the active `news_bias_312` environment on 2026-04-08.
#
# Note:
# - `torch` and `torchvision` below reflect the currently installed CUDA 12.8
#   builds in the local environment. If you recreate this environment on a
#   different machine, install matching PyTorch wheels for that CUDA/runtime.

torch==2.12.0.dev20260311+cu128
torchvision==0.26.0.dev20260311+cu128
transformers==5.3.0
datasets==4.8.4
trl==1.0.0
peft==0.18.1
accelerate==1.13.0
safetensors==0.7.0

numpy==1.26.4
scikit-learn==1.8.0
PyYAML==6.0.3
pydantic==2.12.5
Pillow==12.0.0
tqdm==4.67.3
tensorboard==2.20.0
